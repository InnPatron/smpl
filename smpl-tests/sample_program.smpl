struct Point {
	x: i32,
	y: i32,
}

struct Graph {
	p: Point,
}

fn test_0() {
	let foo: String = "Hello world";
	let bar: String = "Hiya";

	foo = bar;
}

fn test_1(a: i32, b: i32) {
	let a: i32 = a;
	let b: i32 = b;

	let c: i32 = 5 + 6;

	if a == 5 {
		test_0();
	}

	c = a + 1;
}

fn main() {

	let point: Point = init Point {
		x: 5,
		y: 10,
	};

	let graph: Graph = init Graph {
		p: point,
	};

	test_1(point.x, point.y);

	graph.p.x = 100;

	point.y = 20;

	let test_b: bool = point.x == point.y;
}
